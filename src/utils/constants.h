#pragma once

namespace utils
{
    // Terrain
    const float TERRAIN_MIN_HEIGHT = 0.0;
    const float TERRAIN_MAX_HEIGHT = 6.0;
    const float TERRAIN_SCALE = 0.05;
    const float TERRAIN_GRASS_MIN_HEIGHT = 1.6;

    const float OCEAN_SIZE = 16.0;
    const float OCEAN_HEIGHT = 0.4;
    const float OCEAN_SCALE = 0.1;

    const float MAX_TIME = 100;
}