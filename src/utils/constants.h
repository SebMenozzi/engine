#pragma once

namespace utils
{
    // Heightmap
    const float HEIGHTMAP_MIN_HEIGHT = 0.0;
    const float HEIGHTMAP_MAX_HEIGHT = 10.0;
    const float HEIGHTMAP_SCALE = 0.1;
    const char HEIGHTMAP_ASSET[1024] = "assets/textures/heightmap.png";
}